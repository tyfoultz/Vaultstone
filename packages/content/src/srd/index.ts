import type { ContentResult, ContentQuery } from '@vaultstone/types';

// SRD JSON data files will be added here as they are sourced and formatted.
// All SRD content is CC-BY 4.0 — attribution is displayed in the UI when shown.

export function search(_query: ContentQuery): ContentResult[] {
  // TODO: load and filter bundled SRD JSON data
  return [];
}
