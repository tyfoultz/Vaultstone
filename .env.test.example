# Credentials for Playwright/automated functional checks (Tier 4).
# Copy to `.env.test` (gitignored) and fill in. See docs/dev-workflow.md.
#
# Use a dedicated Supabase user that only has access to throwaway test data.
# Never reuse a real account — automated runs may mutate state.

TEST_USER_EMAIL=
TEST_USER_PASSWORD=
TEST_BASE_URL=http://localhost:8081
